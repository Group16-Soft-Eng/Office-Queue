import React from 'react';

const DisplayBoard: React.FC = () => (
  <div>
    <h2>Display Board</h2>
    {/* Visualizzazione code, ticket chiamati, notifiche */}
  </div>
);

export default DisplayBoard;